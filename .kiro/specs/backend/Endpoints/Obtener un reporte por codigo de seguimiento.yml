info:
  name: Obtener un reporte por codigo de seguimiento
  type: http

http:
  method: GET
  url: http://localhost:3001/api/reportes/REP-001
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
