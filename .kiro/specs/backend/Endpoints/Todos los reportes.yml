info:
  name: Todos los reportes
  type: http

http:
  method: GET
  url: http://localhost:3001/api/reportes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
